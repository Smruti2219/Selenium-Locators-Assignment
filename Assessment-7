Assignment 7 – Mini Project 
Website: https://www.saucedemo.com/ Tasks: 
- Login with valid user credentials. 
- Search/select a product. 
- Add product to cart. 
- Proceed to checkout. 
- Use at least 3 different locator strategies (ID, CSS, XPath, etc.) in your script.

public class Assessmen_07 {

	public static void main(String[] args) throws InterruptedException {
      
		ChromeOptions options = new ChromeOptions();
		options.addArguments("--incognito");               // fresh session
		options.addArguments("--guest");                  // guest profile
		

		WebDriver driver = new ChromeDriver(options);
        driver.manage().window().maximize();
        driver.get("https://www.saucedemo.com/");

        // --- Step 1---
        // Using ID locator
        driver.findElement(By.id("user-name")).sendKeys("visual_user");
        driver.findElement(By.id("password")).sendKeys("secret_sauce");
        driver.findElement(By.id("login-button")).click();

        Thread.sleep(2000);
        // --- Step 2---
        // Using CSS locator (select Sauce Labs Backpack)
        WebElement product = driver.findElement(By.cssSelector("div.inventory_item_name"));
        product.click();
        
        Thread.sleep(2000);
        // --- Step 3---
        // Using XPath locator
        driver.findElement(By.xpath("//button[text()='Add to cart']")).click();

        Thread.sleep(2000);
        // --- Step 4---
        driver.findElement(By.className("shopping_cart_link")).click();
        driver.findElement(By.id("checkout")).click();
        
        Thread.sleep(2000);
        // --- Step---
        driver.findElement(By.id("first-name")).sendKeys("John");
        driver.findElement(By.id("last-name")).sendKeys("Doe");
        driver.findElement(By.id("postal-code")).sendKeys("12345");
        driver.findElement(By.id("continue")).click();
        
        Thread.sleep(2000);
        // --- Step 6---
        driver.findElement(By.id("finish")).click();

        // Verification message
        String msg = driver.findElement(By.className("complete-header")).getText();
        System.out.println("Checkout Message: " + msg);
        Thread.sleep(2000);
        driver.quit();
    }
}
